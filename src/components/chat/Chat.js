import { connect } from "react-redux";
import Header from '../header/Header'

const Chat = (props) => {
  return (
    <div>
      <Header />
      Hello World! This is the Chat Page.
    </div>
  )
}

export default Chat;